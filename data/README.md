# Data directory

This directory is intentionally empty. Per-week scripts download or cache
their datasets here on first run; nothing is shipped with the repository.
Use the per-week notebooks to populate the relevant subdirectories.
